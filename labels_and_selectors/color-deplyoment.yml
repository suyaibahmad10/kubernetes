apiVersion: apps/v1
kind: Deployment
metadata:
  name: color-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: color-api
      environment: local
      tier: backend
    matchExpressions:
      - key: app
        operator: In
        values:
          - color-api
  template:
    metadata:
      labels:
        app: color-api
        environment: local
        tier: backend
    spec:
      containers:
        - name: color-api
          image: lmacademy/color-api:1.1.0
          ports:
            - containerPort: 80